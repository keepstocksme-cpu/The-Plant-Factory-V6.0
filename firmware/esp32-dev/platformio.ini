[env:esp32dev]
platform = espressif32
board = esp32dev
framework = arduino
monitor_speed = 115200
upload_speed = 921600

; WiFi.h and WebServer.h are provided by the Arduino framework for ESP32.
; No extra libraries are required for this first test.
