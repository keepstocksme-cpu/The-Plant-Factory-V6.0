# Web Notes

This documentation folder is for the future React dashboard package.

## Current status

- The working React app still lives at the repository root
- Thai, Lao, and English support already exist in the current app
- The current UI is already mobile-aware

## Future move

In a later phase, the root React app can be moved into `web/` with minimal config changes.

## Web goals

- Keep the current dashboard working
- Preserve Thai, Lao, and English support
- Keep the UI mobile-friendly
- Connect pump controls to the ESP32 firmware API

